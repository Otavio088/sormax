# Configuração do ambiente Flask
FLASK_ENV=development
FLASK_APP=icecreampy/app.py
FLASK_DEBUG=1

# Chave secreta do Flask (substituir por um valor seguro)
SECRET_KEY=SUA_CHAVE_SECRETA

# URI de conexão com o banco de dados (substituir com credenciais reais)
DATABASE_URI=mysql://USUARIO:SENHA@localhost:3306/NOME_DO_BANCO